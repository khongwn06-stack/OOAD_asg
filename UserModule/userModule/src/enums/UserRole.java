package enums;

/**
 * Fixed set of user roles in the Smart Metro Ticketing System.
 * Using an enum guarantees only valid role values can exist
 * (demonstrates the "Enum" requirement of the assignment).
 */
public enum UserRole {
    PASSENGER,
    ADMIN
}
